version: '3.8'

services:
  orbit:
    image: ghcr.io/waldokilian2/orbit:latest
    container_name: orbit-dashboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./config/:/app/config/:rw
